# Разработка КАР-точки

Краткая инструкция для тех, кто дорабатывает проект: настройка окружения, git, тесты и локальный запуск.

## Первоначальная настройка

1. **Склонируйте репозиторий** (или откройте папку проекта в Cursor).

2. **Создайте локальный конфиг Supabase** — файл `js/config.js` не хранится в git:
   ```bash
   cp js/config.example.js js/config.js
   ```
   Откройте `js/config.js` и впишите `SUPABASE_URL` и `SUPABASE_ANON_KEY` из панели Supabase.  
   Если ключи оставить пустыми, приложение работает только в **демо-режиме** (IndexedDB).

3. **Установите зависимости** (нужен [Node.js](https://nodejs.org/) 18+):
   ```bash
   npm install
   ```

## Локальный запуск приложения

Исходники — **TypeScript** (`js/**/*.ts`). Перед запуском нужна компиляция `tsc` (её делает `npm run dev` / `npm run build`). В dev бандлер не нужен — отдаются скомпилированные `js/**/*.js`.

**Вариант 1 — dev-сервер проекта (рекомендуется):**
```bash
npm run dev
```
Откройте в браузере адрес из терминала (обычно http://localhost:8080).

Помимо статики dev-сервер выполняет и серверные функции YouTube-импорта
(`/api/yt-video`, `/api/yt-generate`) — «Карточки из YouTube» работают локально.
Ключи берутся из настроек приложения (Настройки → «Карточки из YouTube») или из
локального `.env` (см. `.env.example`). Простой `python3 -m http.server` для этого
не подходит: он не умеет POST и вернёт «Ошибка сервера (501)».

**Вариант 2 — двойной клик по `index.html`:**  
Демо-режим обычно работает, но ES-модули и service worker могут вести себя иначе без localhost — для полной проверки используйте сервер выше.

**Прод-сборка:** `npm run build:bundle` → `dist/` (esbuild + `dist/sw.js`).

**Chrome-расширение:** `npm run ext:build`, затем Load unpacked из `extension/` (артефакты в `extension/dist/`, не коммитятся).

## Тесты

Unit-тесты (Vitest): SRS (`js/lib/srs.ts`), sync, UI-хелперы, SW precache и др. — порядка **370+** тестов в `tests/*.test.js`.

**Запустить один раз (CI / перед коммитом):**
```bash
npm test
```

**Запустить в режиме наблюдения** (перезапуск при изменении файлов):
```bash
npm run test:watch
```

При успехе вы увидите что-то вроде:
```
 ✓ tests/srs.test.js (14 tests)
 …
 Test Files  61 passed
```

Также: `npm run typecheck`, `npm run lint`, `npm run test:e2e` (Playwright).

## Работа с Git

### Посмотреть изменения
```bash
git status          # какие файлы изменены
git diff            # что именно изменилось
git log --oneline   # история коммитов
```

### Сохранить изменения (коммит)

```bash
# Добавить нужные файлы (js/config.js добавлять НЕ нужно — он в .gitignore)
git add .

# Создать коммит с сообщением
git commit -m "Кратко опишите, зачем это изменение"
```

Примеры сообщений:
- `fix: исправить подсчёт карточек к повторению`
- `feat: добавить офлайн-очередь для облака`
- `refactor: разбить app.js на модули`

### Отправить на GitHub (push)

```bash
# Первый раз для новой ветки
git push -u origin имя-ветки

# Дальше, если ветка уже привязана к remote
git push
```

Если работаете в `main`:
```bash
git push origin main
```

### Типичный цикл разработки

```bash
git pull                    # подтянуть свежие изменения с сервера
# ... правки в коде ...
npm test                    # убедиться, что тесты проходят
git add .
git commit -m "описание"
git push
```
Проверить изменения локально без деплоя:
```bash
npm run dev
```

### Важно

- **Не коммитьте** `js/config.js` — там ключи Supabase. В репозитории лежит только `js/config.example.js`.
- **Не коммитьте** `node_modules/`, `dist/`, `extension/dist/`, архивы деплоя, `__pycache__/`.
- Перед push желательно прогнать `npm test` и `npm run typecheck`.

## Структура проекта

```
js/                     — исходники TypeScript (компиляция на место в js/**/*.js)
  app.ts                — точка входа
  config.example.js
  core/                 — state, router
  data/                 — LocalStore / CloudStore, sync-queue, SRS meta
  lib/                  — srs.ts, activity, debounce, …
  ui/                   — shell, rich-editor, helpers
  screens/              — home, folder, review, settings, …
tests/*.test.js         — Vitest (~60 файлов)
functions/              — Cloudflare Pages Functions
extension/              — Chrome-расширение (src/ → dist/ через ext:build)
```
Подробнее: [CLAUDE.md](CLAUDE.md), [README.md](README.md).
